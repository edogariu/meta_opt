from setuptools import setup, find_packages

setup(
   name='meta_opt',
   version='1.0',
   description='for running meta optimization',
   author='someone anonymous :)  (we submitted the code with the paper and are under review)',
   packages=find_packages(),
   install_requires=['wheel',],  #external packages as dependencies
)
